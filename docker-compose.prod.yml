services:
  desarrollo-services:    
    image: ${NAME_SERVICE}-latest
    container_name: ${NAME_SERVICE}-frontend
    networks:
      - luis
    restart: always

networks:
  luis:
    external: true