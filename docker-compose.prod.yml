services:
  desarrollo-services:    
    image: ${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/syssoftintegra/front-end-development:${NAME_SERVICE}-latest
    container_name: ${NAME_SERVICE}-frontend
    networks:
      - luis
    restart: always

networks:
  luis:
    external: true