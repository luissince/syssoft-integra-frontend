import ContainerWrapper from "@/components/ui/container-wrapper";
import MainDashboard from "@/components/view/dashboard/MainDashboard";

const Dashboard = () => {
  return (
    <ContainerWrapper>
      <MainDashboard />
    </ContainerWrapper>
  );
};

export default Dashboard;